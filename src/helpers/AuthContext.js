//libraries
import { createContext } from "react";

//generating our contexts using the createContext function
//contexts will be used to store global states
export const AuthContext = createContext({});

export const UserContext = createContext({});
